import type { HighlightOptions } from '../types';

export const DEFAULT_HIGHLIGHT_OPTIONS: HighlightOptions = {
  caseSensitive: false,
  splitBySpace: true,
  highlightTag: 'mark',
  highlightClass: '',
  highlightStyle: '',
  wrapperTag: 'button',
  wrapperClass: '',
};
